import { useState, useEffect } from "react";

/* eslint-disable react/prop-types */
const Pagination = ({ dataProduct }) => {
  return (
    <div className="mx-auto w-1/4">
      {/* {table.map((page) => {
        return <li key={page}>{page}sss</li>;
      })} */}
    </div>
  );
};

export default Pagination;
